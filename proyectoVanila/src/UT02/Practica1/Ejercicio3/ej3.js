"use strict";

function iteracionNumeros(numero1, numero2) {
    let total = numero2;
    let array = new Array[numero1];
    array[0] = numero2;
    for (let i = 1; i < numero1; i++) {
        array[i]
    }
}

export {iteracionNumeros};